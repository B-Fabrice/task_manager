import 'package:flutter/material.dart';

// common colors
const Color white = Color(0xFFFFFFFF);
const Color primary = Color(0xFF001427);
const Color secondary = Color(0xFF001427);
const Color red = Color(0xFFEA5455);

// light theme colors
const Color background = Color(0xFFFFF8F0);
const Color text = Color(0xFF001427);

// dark theme colors
const Color darkBackground = Color(0xFF001427);
const Color textDark = Color(0xFFFFF8F0);
