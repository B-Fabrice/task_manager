import 'package:flutter/material.dart';

// common colors

// light theme colors
const Color background = Color(0xFFFFF8F0);
const Color text = Color(0xFF001427);

// dark theme colors
const Color backgroundDark = Color(0xFF001427);
const Color textDark = Color(0xFFFFF8F0);
